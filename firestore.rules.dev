rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Open rules for development - DO NOT USE IN PRODUCTION
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
